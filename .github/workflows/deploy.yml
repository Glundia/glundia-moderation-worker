name: Build and Deploy Moderation Worker

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - prod

env:
  REGION: us-central1
  SERVICE_NAME: moderation-worker

jobs:
  build:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    outputs:
      image-tag: ${{ steps.build.outputs.image-tag }}
      full-image: ${{ steps.build.outputs.full-image }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get commit SHA
        id: sha
        run: echo "sha=${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Determine image tag
        id: build
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/develop" ]]; then
            echo "image-tag=develop-${{ steps.sha.outputs.sha }}" >> $GITHUB_OUTPUT
          else
            echo "image-tag=latest" >> $GITHUB_OUTPUT
          fi
        shell: bash

      - name: Authenticate to Google Cloud (DEV)
        if: github.ref == 'refs/heads/develop'
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER_DEV }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT_DEV }}

      - name: Authenticate to Google Cloud (PROD)
        if: github.ref == 'refs/heads/main'
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER_PROD }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT_PROD }}

      - name: Configure Docker
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build and push Docker image (DEV)
        if: github.ref == 'refs/heads/develop'
        run: |
          PROJECT_ID=${{ secrets.GCP_PROJECT_ID_DEV }}
          docker build -t ${{ env.REGION }}-docker.pkg.dev/$PROJECT_ID/${{ env.SERVICE_NAME }}/${{ env.SERVICE_NAME }}:${{ steps.build.outputs.image-tag }} .
          docker push ${{ env.REGION }}-docker.pkg.dev/$PROJECT_ID/${{ env.SERVICE_NAME }}/${{ env.SERVICE_NAME }}:${{ steps.build.outputs.image-tag }}
          echo "full-image=${{ env.REGION }}-docker.pkg.dev/$PROJECT_ID/${{ env.SERVICE_NAME }}/${{ env.SERVICE_NAME }}:${{ steps.build.outputs.image-tag }}" >> $GITHUB_OUTPUT

      - name: Build and push Docker image (PROD)
        if: github.ref == 'refs/heads/main'
        run: |
          PROJECT_ID=${{ secrets.GCP_PROJECT_ID_PROD }}
          docker build -t ${{ env.REGION }}-docker.pkg.dev/$PROJECT_ID/${{ env.SERVICE_NAME }}/${{ env.SERVICE_NAME }}:${{ steps.build.outputs.image-tag }} .
          docker push ${{ env.REGION }}-docker.pkg.dev/$PROJECT_ID/${{ env.SERVICE_NAME }}/${{ env.SERVICE_NAME }}:${{ steps.build.outputs.image-tag }}
          echo "full-image=${{ env.REGION }}-docker.pkg.dev/$PROJECT_ID/${{ env.SERVICE_NAME }}/${{ env.SERVICE_NAME }}:${{ steps.build.outputs.image-tag }}" >> $GITHUB_OUTPUT

  deploy-dev:
    name: Deploy to DEV
    needs: build
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    environment:
      name: dev
    steps:
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER_DEV }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT_DEV }}

      - name: Deploy to Cloud Run (DEV)
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image=${{ needs.build.outputs.full-image }} \
            --project=${{ secrets.GCP_PROJECT_ID_DEV }} \
            --region=${{ env.REGION }} \
            --platform=managed \
            --service-account=moderation-worker@${{ secrets.GCP_PROJECT_ID_DEV }}.iam.gserviceaccount.com \
            --memory=512Mi \
            --cpu=1 \
            --min-instances=0 \
            --max-instances=10 \
            --timeout=300 \
            --set-env-vars=GCP_PROJECT=${{ secrets.GCP_PROJECT_ID_DEV }},ENVIRONMENT=dev,PUBSUB_SUBSCRIPTION=moderation-worker-sub,QUARANTINE_BUCKET=glundia-dev-uploads-quarantine,PUBLIC_BUCKET=glundia-dev-images-public,REJECTED_BUCKET=glundia-dev-images-rejected,DATABASE_URL=${{ secrets.DEV_DATABASE_URL }}

  deploy-prod:
    name: Deploy to PROD
    needs: build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: prod
    steps:
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER_PROD }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT_PROD }}

      - name: Deploy to Cloud Run (PROD)
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image=${{ needs.build.outputs.full-image }} \
            --project=${{ secrets.GCP_PROJECT_ID_PROD }} \
            --region=${{ env.REGION }} \
            --platform=managed \
            --service-account=moderation-worker@${{ secrets.GCP_PROJECT_ID_PROD }}.iam.gserviceaccount.com \
            --memory=1Gi \
            --cpu=2 \
            --min-instances=1 \
            --max-instances=100 \
            --timeout=300 \
            --set-env-vars=GCP_PROJECT=${{ secrets.GCP_PROJECT_ID_PROD }},ENVIRONMENT=prod,PUBSUB_SUBSCRIPTION=moderation-worker-sub,QUARANTINE_BUCKET=glundia-prod-uploads-quarantine,PUBLIC_BUCKET=glundia-prod-images-public,REJECTED_BUCKET=glundia-prod-images-rejected,DATABASE_URL=${{ secrets.PROD_DATABASE_URL }}

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov

      - name: Run unit tests
        run: pytest tests/unit/ -v --cov=. --cov-report=xml

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          flags: unittests
